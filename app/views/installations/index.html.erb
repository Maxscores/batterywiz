<div class="row">
  <div class="col col-2">
    <p></p>
  </div>
  <div class="col col-8">
    <table class="data-table">
      <th>Installation ID</th>
      <th>Solar Capacity</th>
      <th>Location</th>
      <% @installations.each do |installation| %>
      <tr>
        <td><%= installation.id %></td>
        <td><%= installation.solar_system.capacity %></td>
        <td><%= installation.zipcode %></td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
